<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical">

        <TextView
            android:id="@+id/select_sex_male"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dp_forty_eight"
            android:background="@android:color/white"
            android:gravity="center"
            android:text="@string/select_sex_male"
            android:textColor="@color/colorText"
            android:textSize="@dimen/sp_fifteen" />

        <View
            android:layout_width="match_parent"
            android:layout_height="@dimen/dp_zero_five"
            android:background="@color/colorLine" />

        <TextView
            android:id="@+id/select_sex_female"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dp_forty_eight"
            android:background="@android:color/white"
            android:gravity="center"
            android:text="@string/select_sex_female"
            android:textColor="@color/colorText"
            android:textSize="@dimen/sp_fifteen" />

        <TextView
            android:id="@+id/select_sex_cancel"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dp_forty_eight"
            android:layout_marginTop="@dimen/dp_eight"
            android:background="@android:color/white"
            android:gravity="center"
            android:text="@string/cancel"
            android:textColor="@color/colorTheme"
            android:textSize="@dimen/sp_fifteen" />
    </LinearLayout>
</RelativeLayout>
